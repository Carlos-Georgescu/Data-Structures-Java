//Rerfer to the List Abstract for cost of operation

int[] name = new int[size];// initiate the array

int[] name = new int[]{Integer, Integer,Integer}//initiating the array with three integer values

//or 

int[] name = {Integer, Integer, Integer}//does the exact same thing as above

name.length//finding the lengher of the array

int[].. name new int[]..;//you can always do nested arrays

Arrays.sort(name);//this is pass by reference for some reason

Arrays.sort(name, start, end);//this sorts only a given section of the array

